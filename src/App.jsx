import React from 'react';
import TodoList from './TodoList.jsx'; // Assuming the file is in the same directory

const App = () => {
  return (
    <div>
      <TodoList />
    </div>
  );
};

export default App;
